<!--
  DIESE DATEI WIRD NICHT LIVE AUSGELIEFERT.

  Die aktive Sitemap wird dynamisch vom Express-Server generiert:
    server/sitemap.ts  →  Route: app.get('/sitemap.xml', handleSitemap)

  Diese statische Datei wird durch die Server-Route überschrieben und
  hat KEINE Auswirkung auf medicalcaregermany.com/sitemap.xml.

  Bitte alle Sitemap-Änderungen ausschließlich in server/sitemap.ts vornehmen.

  ───────────────────────────────────────────────────────────────────────────

  THIS FILE IS NOT SERVED LIVE.

  The active sitemap is dynamically generated by the Express server:
    server/sitemap.ts  →  Route: app.get('/sitemap.xml', handleSitemap)

  This static file is overridden by the server route and has NO effect
  on the live website. All sitemap changes must be made in server/sitemap.ts.
-->
